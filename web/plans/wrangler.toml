# Cloudflare Worker serving the public Witself plan catalog (plans.json).
# Deploy from infra/cloudflare/control-plane with its committed Wrangler lock:
#   npm ci && npm run deploy:plans
name = "witself-plans"
main = "index.js"
compatibility_date = "2026-06-01"
