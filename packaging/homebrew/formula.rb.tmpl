# This file was generated by Witself release tooling. DO NOT EDIT.
class {{ .ClassName }} < Formula
  desc "{{ .Description }}"
  homepage "https://github.com/witwave-ai/witself"
  version "{{ .Version }}"
  license "FSL-1.1-ALv2"
{{ if .Dependency }}
  depends_on "{{ .Dependency }}"
{{ end }}
  on_macos do
    if Hardware::CPU.intel?
      url "{{ .DarwinAMD64.URL }}"
      sha256 "{{ .DarwinAMD64.SHA256 }}"

      define_method(:install) do
        bin.install "{{ .Binary }}"
{{- if .Alias }}
        bin.install_symlink "{{ .Binary }}" => "{{ .Alias }}"
{{- end }}
      end
    end
    if Hardware::CPU.arm?
      url "{{ .DarwinARM64.URL }}"
      sha256 "{{ .DarwinARM64.SHA256 }}"

      define_method(:install) do
        bin.install "{{ .Binary }}"
{{- if .Alias }}
        bin.install_symlink "{{ .Binary }}" => "{{ .Alias }}"
{{- end }}
      end
    end
  end

  on_linux do
    if Hardware::CPU.intel? && Hardware::CPU.is_64_bit?
      url "{{ .LinuxAMD64.URL }}"
      sha256 "{{ .LinuxAMD64.SHA256 }}"

      define_method(:install) do
        bin.install "{{ .Binary }}"
{{- if .Alias }}
        bin.install_symlink "{{ .Binary }}" => "{{ .Alias }}"
{{- end }}
      end
    end
    if Hardware::CPU.arm? && Hardware::CPU.is_64_bit?
      url "{{ .LinuxARM64.URL }}"
      sha256 "{{ .LinuxARM64.SHA256 }}"

      define_method(:install) do
        bin.install "{{ .Binary }}"
{{- if .Alias }}
        bin.install_symlink "{{ .Binary }}" => "{{ .Alias }}"
{{- end }}
      end
    end
  end

  test do
    system bin/"{{ .Binary }}", "{{ .TestArgument }}"
  end
end
