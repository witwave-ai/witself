// Package server owns server config, lifecycle, health, and migrations for the
// witself-server binary, including the separate API (:8080), health (:8081),
// and metrics (:9090) ports.
package server
