# Synthetic validator fixture

`valid-release-matrix.json` is test-only fabricated data, not execution or release evidence. Its fixed version is 9.8.7, commit is forty `a` characters, repository is witwave-ai/witself, run ID is 100 and attempt is 1. Hashes are derived from clearly synthetic labels. Build workflows must never copy this fixture as a report.
