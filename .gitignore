# macOS / OS
.DS_Store
Thumbs.db

# Editors
*.swp
*~
.idea/
.vscode/

# Go (implementation to come — see docs/scaffold-readiness.md)
/bin/
/dist/
*.test
*.out
coverage.*

# Local env / credentials (never commit)
.env
*.local
*.token

# Terraform — never commit state, lockfiles for state, crash logs, or real vars
# (see infra/terraform/README.md and docs/terraform-infrastructure.md)
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
*.tfvars
*.tfvars.json
!*.tfvars.example
.terraformrc
terraform.rc
