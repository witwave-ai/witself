diff --git a/.gitops/cells/civo-sandbox-use1-serving/values.yaml b/.gitops/cells/civo-sandbox-use1-serving/values.yaml
index e1c029c7..5c59457c 100644
--- a/.gitops/cells/civo-sandbox-use1-serving/values.yaml
+++ b/.gitops/cells/civo-sandbox-use1-serving/values.yaml
@@ -34,8 +34,11 @@ apps:
       enabled: true
       allowExternal: false
     metrics:
-      # Keep exporter ingress closed until the monitoring CRDs are healthy.
-      enabled: false
+      enabled: true
+      serviceMonitor:
+        enabled: true
+        labels:
+          release: witself-monitoring
   witselfServer:
     enabled: true
     namespace: witself
@@ -62,6 +65,21 @@ apps:
       validationEnabled: false
       targetName: witself-backup
     replicaCount: 2
+    # Phase 2 of the monitoring rollout: expose server metrics to the
+    # monitoring-namespace Prometheus only.
+    metrics:
+      serviceMonitor:
+        enabled: true
+        labels:
+          release: witself-monitoring
+    networkPolicy:
+      metricsFrom:
+        - namespaceSelector:
+            matchLabels:
+              kubernetes.io/metadata.name: monitoring
+          podSelector:
+            matchLabels:
+              app.kubernetes.io/name: prometheus
     podDisruptionBudget:
       enabled: true
       minAvailable: 1
@@ -87,6 +105,20 @@ apps:
         enabled: true
         mode: enforce
       replicaCount: 2
+      # Phase 2: worker metrics restricted to the same Prometheus peers.
+      metrics:
+        serviceMonitor:
+          enabled: true
+          labels:
+            release: witself-monitoring
+      networkPolicy:
+        metricsFrom:
+          - namespaceSelector:
+              matchLabels:
+                kubernetes.io/metadata.name: monitoring
+            podSelector:
+              matchLabels:
+                app.kubernetes.io/name: prometheus
       podDisruptionBudget:
         enabled: true
         minAvailable: 1
@@ -136,8 +168,8 @@ apps:
         targetName: witself-provision
         tokenKey: token
 platform:
-  # Recovery phase 1: install the stack and CRDs with null-routed Alertmanager.
-  # Scrape discovery and receiver routing follow in separate GitOps changes.
+  # Recovery phase 2: discover PostgreSQL, server, and worker scrape targets.
+  # Keep Alertmanager null-routed until sustained scrapes are verified.
   monitoring:
     enabled: true
     alerting:
diff --git a/internal/gitopsvalues/overlays/civo-sandbox-use1-serving.yaml.tmpl b/internal/gitopsvalues/overlays/civo-sandbox-use1-serving.yaml.tmpl
index a9714bcd..4564dfd9 100644
--- a/internal/gitopsvalues/overlays/civo-sandbox-use1-serving.yaml.tmpl
+++ b/internal/gitopsvalues/overlays/civo-sandbox-use1-serving.yaml.tmpl
@@ -20,8 +20,11 @@ apps:
       enabled: true
       allowExternal: false
     metrics:
-      # Keep exporter ingress closed until the monitoring CRDs are healthy.
-      enabled: false
+      enabled: true
+      serviceMonitor:
+        enabled: true
+        labels:
+          release: witself-monitoring
   witselfServer:
     enabled: true
     namespace: witself
@@ -48,6 +51,21 @@ apps:
       validationEnabled: false
       targetName: witself-backup
     replicaCount: 2
+    # Phase 2 of the monitoring rollout: expose server metrics to the
+    # monitoring-namespace Prometheus only.
+    metrics:
+      serviceMonitor:
+        enabled: true
+        labels:
+          release: witself-monitoring
+    networkPolicy:
+      metricsFrom:
+        - namespaceSelector:
+            matchLabels:
+              kubernetes.io/metadata.name: monitoring
+          podSelector:
+            matchLabels:
+              app.kubernetes.io/name: prometheus
     podDisruptionBudget:
       enabled: true
       minAvailable: 1
@@ -73,6 +91,20 @@ apps:
         enabled: true
         mode: enforce
       replicaCount: 2
+      # Phase 2: worker metrics restricted to the same Prometheus peers.
+      metrics:
+        serviceMonitor:
+          enabled: true
+          labels:
+            release: witself-monitoring
+      networkPolicy:
+        metricsFrom:
+          - namespaceSelector:
+              matchLabels:
+                kubernetes.io/metadata.name: monitoring
+            podSelector:
+              matchLabels:
+                app.kubernetes.io/name: prometheus
       podDisruptionBudget:
         enabled: true
         minAvailable: 1
@@ -122,8 +154,8 @@ apps:
         targetName: witself-provision
         tokenKey: token
 platform:
-  # Recovery phase 1: install the stack and CRDs with null-routed Alertmanager.
-  # Scrape discovery and receiver routing follow in separate GitOps changes.
+  # Recovery phase 2: discover PostgreSQL, server, and worker scrape targets.
+  # Keep Alertmanager null-routed until sustained scrapes are verified.
   monitoring:
     enabled: {{.Monitoring}}
     alerting:
