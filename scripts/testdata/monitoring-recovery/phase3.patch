diff --git a/.gitops/cells/catalog.yaml b/.gitops/cells/catalog.yaml
index 59fdd373..8abf0e60 100644
--- a/.gitops/cells/catalog.yaml
+++ b/.gitops/cells/catalog.yaml
@@ -62,6 +62,6 @@ cells:
       domain_documentation_only: true
       monitoring: true
       collector_alerts: false
-      sealed_plane_alerts: false
+      sealed_plane_alerts: true
       postgres_backup: true
       memory_alerts: false
diff --git a/.gitops/cells/civo-sandbox-use1-serving/values.yaml b/.gitops/cells/civo-sandbox-use1-serving/values.yaml
index e79f9b17..d1d61c89 100644
--- a/.gitops/cells/civo-sandbox-use1-serving/values.yaml
+++ b/.gitops/cells/civo-sandbox-use1-serving/values.yaml
@@ -194,20 +194,44 @@ apps:
         targetName: witself-provision
         tokenKey: token
 platform:
-  # Recovery phase 2: discover PostgreSQL, server, and worker scrape targets.
-  # Keep Alertmanager null-routed until sustained scrapes are verified.
+  # Phase 3 of the monitoring rollout (runbook: Founder open-plane monitoring).
+  # Stack (phase 1) and scrape targets (phase 2) are verified; this enables
+  # alert routing: PagerDuty Events API v2 incidents plus the external
+  # dead-man heartbeat. Secret values live only in the two immutable
+  # in-cluster Secrets named here; nothing sensitive enters Git or Argo state.
   monitoring:
     enabled: true
     alerting:
-      enabled: false
-    collectorAlerts:
-      enabled: false
+      enabled: true
     sealedPlaneAlerts:
-      enabled: false
+      enabled: true
     memoryAlerts:
       enabled: false
     postgresBackupAlerts:
       enabled: true
+    postgresql:
+      enabled: true
+    nodeExporter:
+      enabled: true
+    kubelet:
+      cadvisor: true
+    defaultRules:
+      enabled: true
+    certManager:
+      enabled: true
+    argocd:
+      enabled: true
+    uptimeProbes:
+      enabled: true
+    entitlementDelivery:
+      enabled: true
+    receiver:
+      kind: pagerduty
+      secretName: witself-monitoring-pagerduty-v1
+      secretKey: routing_key
+    receiverDeadman:
+      secretName: witself-monitoring-deadman-v1
+      secretKey: url
   awsAlbIngress:
     enabled: false
   certManager:
diff --git a/internal/gitopsvalues/overlays/civo-sandbox-use1-serving.yaml.tmpl b/internal/gitopsvalues/overlays/civo-sandbox-use1-serving.yaml.tmpl
index fba57d5d..67bc09f5 100644
--- a/internal/gitopsvalues/overlays/civo-sandbox-use1-serving.yaml.tmpl
+++ b/internal/gitopsvalues/overlays/civo-sandbox-use1-serving.yaml.tmpl
@@ -180,20 +180,61 @@ apps:
         targetName: witself-provision
         tokenKey: token
 platform:
-  # Recovery phase 2: discover PostgreSQL, server, and worker scrape targets.
-  # Keep Alertmanager null-routed until sustained scrapes are verified.
+{{- if .Monitoring }}
+  # Phase 3 of the monitoring rollout (runbook: Founder open-plane monitoring).
+  # Stack (phase 1) and scrape targets (phase 2) are verified; this enables
+  # alert routing: PagerDuty Events API v2 incidents plus the external
+  # dead-man heartbeat. Secret values live only in the two immutable
+  # in-cluster Secrets named here; nothing sensitive enters Git or Argo state.
   monitoring:
-    enabled: {{.Monitoring}}
+    enabled: true
     alerting:
-      enabled: false
+      enabled: true
+{{- if .CollectorAlerts }}
     collectorAlerts:
-      enabled: {{.CollectorAlerts}}
+      enabled: true
+{{- end }}
+{{- if .SealedPlaneAlerts }}
     sealedPlaneAlerts:
-      enabled: {{.SealedPlaneAlerts}}
+      enabled: true
+{{- end }}
+    memoryAlerts:
+      enabled: {{.MemoryAlerts}}
+    postgresBackupAlerts:
+      enabled: {{.PostgresBackup}}
+    postgresql:
+      enabled: true
+    nodeExporter:
+      enabled: true
+    kubelet:
+      cadvisor: true
+    defaultRules:
+      enabled: true
+    certManager:
+      enabled: true
+    argocd:
+      enabled: true
+    uptimeProbes:
+      enabled: true
+    entitlementDelivery:
+      enabled: true
+    receiver:
+      kind: pagerduty
+      secretName: witself-monitoring-pagerduty-v1
+      secretKey: routing_key
+    receiverDeadman:
+      secretName: witself-monitoring-deadman-v1
+      secretKey: url
+{{- else }}
+  monitoring:
+    enabled: false
+    alerting:
+      enabled: false
     memoryAlerts:
       enabled: {{.MemoryAlerts}}
     postgresBackupAlerts:
       enabled: {{.PostgresBackup}}
+{{- end }}
   awsAlbIngress:
     enabled: false
   certManager:
diff --git a/internal/gitopsvalues/generate_test.go b/internal/gitopsvalues/generate_test.go
index 9e7fb93d..76b47d54 100644
--- a/internal/gitopsvalues/generate_test.go
+++ b/internal/gitopsvalues/generate_test.go
@@ -74,8 +74,8 @@ func TestSealedPlaneAlertsTemplate(t *testing.T) {
 				}
 				return
 			}
-			if strings.Contains(got, sealedPlaneAlertsBlock) {
-				t.Fatalf("disabled switch enabled sealedPlaneAlerts:\n%s", got)
+			if strings.Contains(got, "sealedPlaneAlerts") {
+				t.Fatalf("disabled switch rendered sealedPlaneAlerts:\n%s", got)
 			}
 		})
 	}
@@ -100,20 +100,22 @@ func TestCatalogSealedPlaneAlertsSwitch(t *testing.T) {
 	if err != nil {
 		t.Fatal(err)
 	}
-	// Alert groups stay off until the monitoring recovery rollout is complete.
 	// The collector group stays off on purpose: WitselfIdentityCapacityAtLimit
 	// would fire permanently because every Personal account already holds its
 	// single root operator seat (used == cap == 1). Only the sealed-plane group
-	// can be enabled in phase 3 on the serving cell.
+	// is enabled on the serving cell.
 	serving := cfg.Cells["civo-sandbox-use1-serving"]
-	if serving.Switches.CollectorAlerts || serving.Switches.SealedPlaneAlerts {
-		t.Fatalf("civo-sandbox-use1-serving switches: collector_alerts=%v sealed_plane_alerts=%v, want false/false",
+	if serving.Switches.CollectorAlerts || !serving.Switches.SealedPlaneAlerts {
+		t.Fatalf("civo-sandbox-use1-serving switches: collector_alerts=%v sealed_plane_alerts=%v, want false/true",
 			serving.Switches.CollectorAlerts, serving.Switches.SealedPlaneAlerts)
 	}
 	for name, cell := range cfg.Cells {
 		if name == "civo-sandbox-use1-serving" {
-			if !cell.Switches.Monitoring || cell.Switches.CollectorAlerts || cell.Switches.SealedPlaneAlerts {
-				t.Error("serving cell must enable monitoring with alert groups off during recovery")
+			// The serving cell enables monitoring and the sealed-plane group,
+			// with the collector group off until the
+			// identity-capacity rule semantics are fixed.
+			if !cell.Switches.Monitoring || cell.Switches.CollectorAlerts || !cell.Switches.SealedPlaneAlerts {
+				t.Error("replacement serving cell must enable monitoring and the sealed-plane group only")
 			}
 			continue
 		}
@@ -133,8 +135,8 @@ func TestGeneratedValuesSealedPlaneAlertsOnlyOnServingCell(t *testing.T) {
 		hasSealed := strings.Contains(body, sealedPlaneAlertsBlock)
 		hasCollector := strings.Contains(body, "    collectorAlerts:\n      enabled: true\n")
 		if cell == "civo-sandbox-use1-serving" {
-			if hasSealed {
-				t.Errorf("%s enables sealedPlaneAlerts before phase 3", valuesRel(cell))
+			if !hasSealed {
+				t.Errorf("%s missing sealedPlaneAlerts block", valuesRel(cell))
 			}
 			if hasCollector {
 				t.Errorf("%s rendered collectorAlerts, which must stay off until the identity-capacity rule semantics are fixed", valuesRel(cell))
@@ -216,7 +218,7 @@ func TestPostgresBackupSingleCatalogSwitch(t *testing.T) {
 						t.Errorf("postgresBackupAlerts.enabled must match postgres_backup=%v", enabled)
 					}
 					wantMonitoring := cell == "civo-sandbox-use1-serving" && monitoringEnabled
-					if monitoring.Enabled != wantMonitoring || monitoring.Alerting.Enabled {
+					if monitoring.Enabled != wantMonitoring || monitoring.Alerting.Enabled != wantMonitoring {
 						t.Errorf("postgres_backup=%v changed independent monitoring or alerting switch", enabled)
 					}
 					if cell == "civo-sandbox-use1-backup" && postgres.Metrics.ServiceMonitor.Enabled {
