diff --git a/.gitops/cells/catalog.yaml b/.gitops/cells/catalog.yaml
index 51106a58..a9a8780a 100644
--- a/.gitops/cells/catalog.yaml
+++ b/.gitops/cells/catalog.yaml
@@ -67,8 +67,8 @@ cells:
     switches:
       domain_documentation_only: true
       monitoring: true
-      collector_alerts: false
-      sealed_plane_alerts: false
+      collector_alerts: true
+      sealed_plane_alerts: true
       postgres_backup: true
   civo-sandbox-usw2-dev:
     cloud: civo
diff --git a/.gitops/cells/civo-sandbox-use1-serving/values.yaml b/.gitops/cells/civo-sandbox-use1-serving/values.yaml
index 72301869..6c233ce4 100644
--- a/.gitops/cells/civo-sandbox-use1-serving/values.yaml
+++ b/.gitops/cells/civo-sandbox-use1-serving/values.yaml
@@ -194,18 +194,44 @@ apps:
         targetName: witself-provision
         tokenKey: token
 platform:
-  # Recovery phase 2: discover PostgreSQL, server, and worker scrape targets.
-  # Keep Alertmanager null-routed until sustained scrapes are verified.
+  # Phase 3 of the monitoring rollout (runbook: Founder open-plane monitoring).
+  # Stack (phase 1) and scrape targets (phase 2) are verified; this enables
+  # alert routing: PagerDuty Events API v2 incidents plus the external
+  # dead-man heartbeat. Secret values live only in the two immutable
+  # in-cluster Secrets named here; nothing sensitive enters Git or Argo state.
   monitoring:
     enabled: true
     alerting:
-      enabled: false
+      enabled: true
     collectorAlerts:
-      enabled: false
+      enabled: true
     sealedPlaneAlerts:
-      enabled: false
+      enabled: true
     postgresBackupAlerts:
       enabled: true
+    postgresql:
+      enabled: true
+    nodeExporter:
+      enabled: true
+    kubelet:
+      cadvisor: true
+    defaultRules:
+      enabled: true
+    certManager:
+      enabled: true
+    argocd:
+      enabled: true
+    uptimeProbes:
+      enabled: true
+    entitlementDelivery:
+      enabled: true
+    receiver:
+      kind: pagerduty
+      secretName: witself-monitoring-pagerduty-v1
+      secretKey: routing_key
+    receiverDeadman:
+      secretName: witself-monitoring-deadman-v1
+      secretKey: url
   awsAlbIngress:
     enabled: false
   certManager:
diff --git a/internal/gitopsvalues/generate_test.go b/internal/gitopsvalues/generate_test.go
index a701b82a..de79ffc2 100644
--- a/internal/gitopsvalues/generate_test.go
+++ b/internal/gitopsvalues/generate_test.go
@@ -113,13 +113,20 @@ func TestCatalogSealedPlaneAlertsSwitch(t *testing.T) {
 		if name == "civo-sandbox-usw2-dev" {
 			continue
 		}
+		if name == "civo-sandbox-use1-serving" {
+			// Recovery phase 3 explicitly enables both groups after scrape verification.
+			if !cell.Switches.Monitoring || !cell.Switches.CollectorAlerts || !cell.Switches.SealedPlaneAlerts {
+				t.Error("replacement serving cell must enable monitoring and both alert groups")
+			}
+			continue
+		}
 		if cell.Switches.SealedPlaneAlerts {
 			t.Errorf("%s unexpectedly sets sealed_plane_alerts", name)
 		}
 	}
 }
 
-func TestGeneratedValuesSealedPlaneAlertsOnlyOnServingCell(t *testing.T) {
+func TestGeneratedValuesSealedPlaneAlertsOnlyOnServingCells(t *testing.T) {
 	generated, err := generateAll(repoRoot(t))
 	if err != nil {
 		t.Fatal(err)
@@ -137,6 +144,12 @@ func TestGeneratedValuesSealedPlaneAlertsOnlyOnServingCell(t *testing.T) {
 			}
 			continue
 		}
+		if cell == "civo-sandbox-use1-serving" {
+			if !hasSealed || !hasCollector {
+				t.Errorf("%s missing recovery phase-3 alert groups", valuesRel(cell))
+			}
+			continue
+		}
 		if hasSealed {
 			t.Errorf("%s unexpectedly rendered sealedPlaneAlerts", valuesRel(cell))
 		}
@@ -168,8 +181,9 @@ func TestCatalogPostgresBackupSwitch(t *testing.T) {
 		t.Fatal(err)
 	}
 	for name, cell := range cfg.Cells {
-		if cell.Switches.PostgresBackup {
-			t.Errorf("%s activates postgres_backup; every checked-in cell must stay dark", name)
+		supported := name == "civo-sandbox-use1-serving" || name == "civo-sandbox-use1-backup"
+		if cell.Switches.PostgresBackup && !supported {
+			t.Errorf("%s activates postgres_backup; only the two reviewed Civo cells may", name)
 		}
 	}
 }
@@ -211,7 +225,7 @@ func TestPostgresBackupSingleCatalogSwitch(t *testing.T) {
 						t.Errorf("postgresBackupAlerts.enabled must match postgres_backup=%v", enabled)
 					}
 					wantMonitoring := cell == "civo-sandbox-use1-serving" && monitoringEnabled
-					if monitoring.Enabled != wantMonitoring || monitoring.Alerting.Enabled {
+					if monitoring.Enabled != wantMonitoring || monitoring.Alerting.Enabled != wantMonitoring {
 						t.Errorf("postgres_backup=%v changed independent monitoring or alerting switch", enabled)
 					}
 					if cell == "civo-sandbox-use1-backup" && postgres.Metrics.ServiceMonitor.Enabled {
@@ -238,7 +252,12 @@ func TestPostgresBackupSingleCatalogSwitch(t *testing.T) {
 }
 
 func TestGeneratedValuesPostgresBackupsDefaultOff(t *testing.T) {
-	generated, err := generateAll(repoRoot(t))
+	root := repoRoot(t)
+	generated, err := generateAll(root)
+	if err != nil {
+		t.Fatal(err)
+	}
+	cfg, err := loadCatalog(root)
 	if err != nil {
 		t.Fatal(err)
 	}
@@ -247,10 +266,11 @@ func TestGeneratedValuesPostgresBackupsDefaultOff(t *testing.T) {
 		backup := values.Apps.CivoPostgres.Backup.Enabled
 		alerts := values.Platform.Monitoring.PostgresBackupAlerts.Enabled
 		if cell == "civo-sandbox-use1-serving" || cell == "civo-sandbox-use1-backup" {
+			want := cfg.Cells[cell].Switches.PostgresBackup
 			if backup == nil || alerts == nil {
-				t.Errorf("%s must explicitly disable both backup and backup alerts", cell)
-			} else if *backup || *alerts {
-				t.Errorf("%s unexpectedly enables backup or backup alerts", cell)
+				t.Errorf("%s must explicitly set both backup and backup alerts", cell)
+			} else if *backup != want || *alerts != want {
+				t.Errorf("%s backup/backup-alert flags must match its catalog switch (%v)", cell, want)
 			}
 		} else if backup != nil || alerts != nil {
 			t.Errorf("%s unexpectedly includes a PostgreSQL backup switch", cell)
diff --git a/internal/gitopsvalues/overlays/civo-sandbox-use1-serving.yaml.tmpl b/internal/gitopsvalues/overlays/civo-sandbox-use1-serving.yaml.tmpl
index 418e34c3..9301e2a0 100644
--- a/internal/gitopsvalues/overlays/civo-sandbox-use1-serving.yaml.tmpl
+++ b/internal/gitopsvalues/overlays/civo-sandbox-use1-serving.yaml.tmpl
@@ -180,18 +180,57 @@ apps:
         targetName: witself-provision
         tokenKey: token
 platform:
-  # Recovery phase 2: discover PostgreSQL, server, and worker scrape targets.
-  # Keep Alertmanager null-routed until sustained scrapes are verified.
+{{- if .Monitoring }}
+  # Phase 3 of the monitoring rollout (runbook: Founder open-plane monitoring).
+  # Stack (phase 1) and scrape targets (phase 2) are verified; this enables
+  # alert routing: PagerDuty Events API v2 incidents plus the external
+  # dead-man heartbeat. Secret values live only in the two immutable
+  # in-cluster Secrets named here; nothing sensitive enters Git or Argo state.
   monitoring:
-    enabled: {{.Monitoring}}
+    enabled: true
     alerting:
-      enabled: false
+      enabled: true
+{{- if .CollectorAlerts }}
     collectorAlerts:
-      enabled: {{.CollectorAlerts}}
+      enabled: true
+{{- end }}
+{{- if .SealedPlaneAlerts }}
     sealedPlaneAlerts:
-      enabled: {{.SealedPlaneAlerts}}
+      enabled: true
+{{- end }}
+    postgresBackupAlerts:
+      enabled: {{.PostgresBackup}}
+    postgresql:
+      enabled: true
+    nodeExporter:
+      enabled: true
+    kubelet:
+      cadvisor: true
+    defaultRules:
+      enabled: true
+    certManager:
+      enabled: true
+    argocd:
+      enabled: true
+    uptimeProbes:
+      enabled: true
+    entitlementDelivery:
+      enabled: true
+    receiver:
+      kind: pagerduty
+      secretName: witself-monitoring-pagerduty-v1
+      secretKey: routing_key
+    receiverDeadman:
+      secretName: witself-monitoring-deadman-v1
+      secretKey: url
+{{- else }}
+  monitoring:
+    enabled: false
+    alerting:
+      enabled: false
     postgresBackupAlerts:
       enabled: {{.PostgresBackup}}
+{{- end }}
   awsAlbIngress:
     enabled: false
   certManager:
