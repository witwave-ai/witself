name: release

# Tag-triggered release: pushing a vX.Y.Z tag builds the binaries, signs the
# checksums (cosign keyless), generates SBOMs, publishes the GitHub release,
# attaches build-provenance attestations, pushes the witself-server multi-arch
# image to GHCR, publishes the witself Helm chart as an OCI artifact to
# ghcr.io/witwave-ai/charts, and pushes the Homebrew formula to witwave-ai/
# homebrew-tap. A manual run (workflow_dispatch) is a no-publish snapshot.
on:
  push:
    tags:
      - "v*"
  workflow_dispatch: {}

permissions:
  contents: write # create the release
  id-token: write # cosign keyless signing + provenance OIDC
  attestations: write # build-provenance attestations
  packages: write # push the witself-server image to GHCR

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: false

      - name: Cache Go release builds
        id: release-go-cache
        uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: release-go-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('go.sum', 'infra/pulumi/go.sum') }}
          restore-keys: |
            release-go-${{ runner.os }}-${{ runner.arch }}-

      - uses: sigstore/cosign-installer@v3
      - uses: anchore/sbom-action/download-syft@v0
      - uses: azure/setup-helm@v4
        with:
          version: v3.16.4
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Free runner disk before GoReleaser
        run: |
          df -h
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL || true
          docker system prune -af || true
          df -h

      - name: Install GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: "~> v2"
          install-only: true

      - name: Run GoReleaser
        env:
          # --parallelism 2: witself-infra (pulumi-aws + pulumi-kubernetes) is a
          # memory-heavy compile; building all targets at once (default = nproc)
          # OOM-killed the runner on v0.0.35. Cap concurrent builds so at most two
          # heavy targets compile simultaneously.
          RELEASE_ARGS: >-
            ${{ github.event_name == 'push' && 'release --clean --parallelism 2'
                || 'release --snapshot --clean --skip=sign --parallelism 2' }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Required for the cross-repo Homebrew formula push (org secret).
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
        run: |
          set -o pipefail

          date +%s > .release-goreleaser-start
          read -r -a goreleaser_args <<< "$RELEASE_ARGS"

          set +e
          goreleaser "${goreleaser_args[@]}" 2>&1 | tee goreleaser.log
          status=${PIPESTATUS[0]}
          set -e

          date +%s > .release-goreleaser-end
          exit "$status"

      - name: Summarize release timings
        if: always()
        env:
          RELEASE_ARGS: >-
            ${{ github.event_name == 'push' && 'release --clean --parallelism 2'
                || 'release --snapshot --clean --skip=sign --parallelism 2' }}
          RELEASE_CACHE_HIT: ${{ steps.release-go-cache.outputs.cache-hit }}
          RELEASE_CACHE_KEY: release-go-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('go.sum', 'infra/pulumi/go.sum') }}
        run: |
          {
            echo "## Release workflow summary"
            echo
            echo "| Item | Value |"
            echo "| --- | --- |"
            echo "| Event | \`${{ github.event_name }}\` |"
            echo "| Ref | \`${{ github.ref_name }}\` |"
            echo "| Commit | \`${{ github.sha }}\` |"
            echo "| GoReleaser args | \`$RELEASE_ARGS\` |"
            echo "| Release Go cache hit | \`${RELEASE_CACHE_HIT:-false}\` |"
            echo "| Release Go cache key | \`$RELEASE_CACHE_KEY\` |"
          } >> "$GITHUB_STEP_SUMMARY"

          if [[ -f .release-goreleaser-start && -f .release-goreleaser-end ]]; then
            start=$(<.release-goreleaser-start)
            end=$(<.release-goreleaser-end)
            elapsed=$((end - start))
            {
              echo "| GoReleaser elapsed | \`${elapsed}s\` |"
              echo
            } >> "$GITHUB_STEP_SUMMARY"
          else
            echo >> "$GITHUB_STEP_SUMMARY"
          fi

          if [[ -f goreleaser.log ]]; then
            perl -pe 's/\e\[[0-9;]*[[:alpha:]]//g' goreleaser.log > goreleaser.clean.log
            awk '
              /took:/ {
                if (match($0, /took: [^[:space:]]+/)) {
                  took = substr($0, RSTART + 6, RLENGTH - 6)
                  if (phase != "") {
                    print phase "|" took
                  }
                }
                next
              }
              /cataloging/ {
                phase = "cataloging"
                next
              }
              /^[[:space:]]*[^[:alnum:][:space:]][[:space:]]+/ {
                line = $0
                sub(/^[[:space:]]*[^[:alnum:][:space:]][[:space:]]+/, "", line)
                if (line != "" && line !~ /^took:/ && line !~ /^cmd=/) {
                  phase = line
                }
              }
            ' goreleaser.clean.log > goreleaser.phases

            if [[ -s goreleaser.phases ]]; then
              {
                echo "### GoReleaser Phases"
                echo
                echo "| Phase | Duration |"
                echo "| --- | ---: |"
                while IFS='|' read -r phase took; do
                  echo "| $phase | \`$took\` |"
                done < goreleaser.phases
                echo
              } >> "$GITHUB_STEP_SUMMARY"
            fi
          fi

          if [[ -f dist/artifacts.json ]]; then
            total=$(jq 'length' dist/artifacts.json)
            archives=$(jq '[.[] | select(.type == "Archive")] | length' dist/artifacts.json)
            sboms=$(jq '[.[] | select(.type == "SBOM")] | length' dist/artifacts.json)
            images=$(jq '[.[] | select(.type == "Docker Image")] | length' dist/artifacts.json)
            {
              echo "### Release Artifacts"
              echo
              echo "| Type | Count |"
              echo "| --- | ---: |"
              echo "| Total | $total |"
              echo "| Archives | $archives |"
              echo "| SBOMs | $sboms |"
              echo "| Docker images | $images |"
            } >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Attest build provenance
        if: github.event_name == 'push'
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: "dist/*.tar.gz"

      # Package the chart with version + appVersion pinned to the release tag, so
      # the chart's default image tag matches the image built in this same run.
      - name: Publish Helm chart (OCI)
        if: github.event_name == 'push'
        run: |
          VER="${GITHUB_REF_NAME#v}"
          echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io -u "${{ github.actor }}" --password-stdin
          helm package charts/witself-server --version "$VER" --app-version "$VER" --destination dist-charts
          helm push "dist-charts/witself-server-${VER}.tgz" oci://ghcr.io/witwave-ai/charts

      - name: Attest chart provenance
        if: github.event_name == 'push'
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: "dist-charts/*.tgz"
